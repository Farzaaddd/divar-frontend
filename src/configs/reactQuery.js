const defaultOptions = {
  defaultOptions: {
    queries: {
      refetchOnWindowFocus: false,
      refetchOnMount: false,
      retry: 1,
      scaleTime: 60 * 1000,
    },
  },
};

export default defaultOptions;
